import { ReplaceSpecialCharsPipe } from './replace-special-chars.pipe';

describe('ReplaceSpecialCharsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSpecialCharsPipe();
    expect(pipe).toBeTruthy();
  });
});
